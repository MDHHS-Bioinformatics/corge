{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/MDHHS-Bioinformatics/corge/main/assets/schema_input.json",
    "title": "MDHHS-Bioinformatics/corge pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces"
            },
            "assembly": {
                "type": "string",
                "pattern": "^\\S+\\.(fa|fna|fas|fasta)$",
                "errorMessage": "Assembly file must be provided, cannot contain spaces and must have extension '.fa', 'fna', 'fas' or '.fasta'"
            },    
            "species": {
                "type": "string",
                "pattern": "^[A-Za-z0-9][A-Za-z0-9\\s\\-_]*$",
                "errorMessage": "Species name must be provided and may contain letters, numbers, spaces, hyphens, or underscores"
            }
        },
        "required": ["sample", "assembly", "species"]
    }
}
